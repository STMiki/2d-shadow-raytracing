/*
** EPITECH PROJECT, 2020
** 2d-shadow-raytracing [WSL: Debian]
** File description:
** core
*/

#pragma once

namespace core {
    #define ACCESS_SPECIFIER(x)
    #define OPE ACCESS_SPECIFIER(operator)
    #define GET ACCESS_SPECIFIER(getter)
    #define SET ACCESS_SPECIFIER(setter)

    #define UNUSED __attribute__((unused))
};
